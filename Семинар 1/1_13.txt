#include <stdio.h>

int main() {
    int x, y;
    printf("Введите координаты точки\n");
    scanf("%d%d", &x, &y); 
    
        if (x>0 && y>0) {
            printf("Точка лежит в первой координатной четверти\n");
            } else { if (x<0 && y<0) {
                printf("Точка лежит в третьей координатной четверти\n");
                } else { if (x>0 && y<0) {
                    printf("Точка лежит в четвертой координатной четверти\n");
                    } else {
                       printf("Точка лежит во второй координатной четверти\n");
          
            }
                    }
             }
         }
