#include <stdio.h>

int main() {
    int a,b,c;
    printf("Введите значения трех отрезков\n");
    scanf("%d%d%d", &a, &b, &c); 
    
        if (((a+b)<=c) || ((a+c)<=b) || ((b+c)<=a)) {
            printf("Треугольника с такими сторонами не существует\n");
            } else { if (a==b || a==c || b==c) {
                printf("Треугольник с такими сторонами существует и является равнобедренным\n");
                } else { if (a==b && a==c && b==c) {
                    printf("Треугольник с такими сторонами существует и является равносторонним\n");
                    } else {
                        printf("Треугольник с такими сторонами существует и является разносторонним\n");
                    } 
          }
             }
         }
